name: Linux x64

on:
  push:
    branches:
      - develop
      - kilted
      - jazzy
      - humble-hawksbill
  workflow_dispatch:

jobs:
  build-and-test:
    uses: ./.github/workflows/linux-x64-build-and-test.yml
    with:
      trigger_type: "push"
    secrets: inherit
