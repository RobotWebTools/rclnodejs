name: Linux ARM64 - Push to Branches

on:
  push:
    branches:
      - develop
      - kilted
      - jazzy
      - humble-hawksbill
  workflow_dispatch:

jobs:
  build-and-test:
    uses: ./.github/workflows/linux-arm64-build-and-test.yml
    with:
      trigger_type: "push"
    secrets: inherit
