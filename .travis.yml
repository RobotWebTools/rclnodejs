services:
  - docker

sudo: required

branches:
  only:
  - develop

script:
  - 'if [ "$DOCKER_USERNAME" != "" ]; then sudo docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD; fi'
  - sudo docker pull ubuntu:focal
  - sudo docker build -t rcldocker .
  - sudo docker run -v $(pwd):/home/rclnodejs/rclnodejs bash -i -c 'cd /home/rclnodejs/rclnodejs && pwd && whoami && ./scripts/build.sh && npm test'
